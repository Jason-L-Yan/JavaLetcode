spring:
  cloud:
    openfeign:
      client:
        config:
          default:
            logger-level: full
            connect-timeout: 1000
            read-timeout: 2000
    sentinel:
      transport:
        dashboard: localhost:8080
      eager: true
      web-context-unify: false
#            request-interceptors:
#              - org.example.order.interceptor.TokenRequestInterceptor
#            retry: feign.Retryer.DEFAULT  # 默认重试次数
#          service-product:
#            logger-level: full
#            connect-timeout: 3000
#            read-timeout: 5000

feign:
  sentinel:
    enabled: true
