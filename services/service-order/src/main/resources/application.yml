order:
  timeout: 800min
  autoConfirm: 700d

spring:
  application:
    name: service-order
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
  config:
    import:
      - nacos:service-order.yml
server:
  port: 8000



